build:
  command: cargo build --target wasm32-wasip2 --release
  component_path: target/wasm32-wasip2/release/service.wasm

dev:
  service: true

  components:
    - name: http-api
      file: target/wasm32-wasip2/release/http_api.wasm

  service_file: service/target/wasm32-wasip2/release/service.wasm