[package]
name = "http-api"
edition = "2024"
version = "0.1.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
shared = { version = "0.1.0", path = "../../shared" }
wash-service-helpers = { version = "0.1.0", path = "../../wash-service-helpers" }
wstd = "0.6"
